class IssueSynthesizer:
    """Parses issue context and produces actionable tasks."""

    def run(self, context):
        # In a real system, parse the context using an LLM
        return {"tasks": [context]}
